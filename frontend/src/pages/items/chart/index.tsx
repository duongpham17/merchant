import { OSRS_GE_TIMESERIES } from '@redux/types/osrs';

interface Props {
  timeseries: OSRS_GE_TIMESERIES[]
}

const ChartIndex = ({timeseries}: Props) => {
  return (
    <div>
      
    </div>
  )
}

export default ChartIndex